import Reach from 'react';

const WarningMessage = () => (
    <div>
        Warning: some option sets contain codes that do not adhere to DHIS2 convention. This could result in invalid codes in the generated code system.
    </div>
);

export default WarningMessage;